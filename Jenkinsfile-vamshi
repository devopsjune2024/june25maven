pipeline {
    agent any
    environment{
        PATH="/opt/maven3.9/bin/:$PATH"
    }
    stages {
        stage('Vamshi-Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/devopsjune2024/june25maven.git'
            }
        }
        
        stage('Vamshi-Build') {
            steps {
                sh 'mvn -B -DskipTests clean package'
            }
        }
        
        stage('Vamshi-Test') {
            steps {
                sh 'mvn test'
            }
            post {
                always{
                    junit 'target/surefire-reports/*.xml'
                }
            }
        }
        
    }
}
